(define (equal1? list1 list2)
  (cond ((not (or (pair? list1)
		   (pair? list2)))
	  (eq? list1 list2))
	 ((and (pair? list1)
	       (pair? list2))
	  (and (equal1? (car list1) (car list2))
	       (equal1? (cdr list1) (cdr list2))))
	 (else #f)))
